[package]
name = "syslog-sign"
description = "Signed syslog messages (RFC 5848) — future"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
