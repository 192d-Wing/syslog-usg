# rsyslog config: forward RFC 5424 messages over TCP with octet-counting
# __TARGET_HOST__ and __TARGET_PORT__ are replaced at test time.

module(load="imuxsock")

# RFC 5424 template
template(name="rfc5424" type="string"
    string="<%PRI%>1 %TIMESTAMP:::date-rfc3339% %HOSTNAME% %APP-NAME% %PROCID% %MSGID% %STRUCTURED-DATA% %msg%\n"
)

# Forward all messages over TCP with octet-counting framing (RFC 6587 §3.4.1)
*.* action(
    type="omfwd"
    target="__TARGET_HOST__"
    port="__TARGET_PORT__"
    protocol="tcp"
    template="rfc5424"
    TCP_Framing="octet-counted"
)
